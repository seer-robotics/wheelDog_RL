# Action space settings.
ABD_OFFSET = 0.0
ABD_ACTION_SCALE = 0.25
HIP_OFFSET = 0.9
HIP_ACTION_SCALE = 0.6
KNEE_OFFSET = -1.8
KNEE_ACTION_SCALE = 0.8
WHEEL_ACTION_SCALE = 15.0

# Observation space settings. 
STATE_HISTORY = 15
SHORT_HISTORY  = 3

# Event RNG settings.
CPU_POOL_BUCKET_SIZE = 256

# Termination settings.
FALL_GRACE_STEPS = 6400
FALL_TILT_DEGREES = 70.0
FALL_TILT_DURATION = 5.0

# Crippled termination settings.
CRIPPLE_FALL_GRACE_STEPS = 8000
CRIPPLE_FALL_TILT_DEGREES = 70.0
CRIPPLE_FALL_TILT_DURATION = 5.0

# Command curriculum settings.
CMD_CURRICULUM_RANGE_PROGRESS_SCALES = {
    "vx": 1.6,
    "omega": 1.6,
    "vy": 1.8
}
CMD_CURRICULUM_TARGET_MAX_RANGES = {
    "vx":    (-1.2, 1.2),
    "vy":    (-0.3, 0.3),
    "omega": (-1.6, 1.6),
}
# CMD_CURRICULUM_INIT_MIN_RANGES = {
#     "vx":    0.3,     # half-width → range [-0.3, 0.3]
#     "vy":    0.0,     # or very small if you want tiny lateral noise
#     "omega": 0.08,    # half-width → range [-0.08, 0.08]
# }

# Terrain curriculum settings.
ANGULAR_ERROR_SCALE = 1.0
CURRICULUM_ERROR_THRESHOLD_UP = 0.4
CURRICULUM_ERROR_THRESHOLD_DOWN = 1.6

# Cripple pose rewards curriculum settings.
CRIPPLE_PENALTY_TARGET_WEIGHT = -2.0
CRIPPLE_PENALTY_FLAT_STEPS = 6400
CRIPPLE_PENALTY_DECAY_STEPS = 6400

ABD_POS_DEVIATE_INIT_WEIGHT = -5.0
ABD_POS_DEVIATE_SCALE_LEVELS = 3
ABD_POS_DEVIATE_MIN_FACTOR = 0.1
ABD_POS_DEVIATE_MIN_FACTOR_TERRAIN_LEVEL = 2
LEG_POS_DEVIATE_INIT_WEIGHT = -1.0
LEG_POS_DEVIATE_SCALE_LEVELS = 3
LEG_POS_DEVIATE_MIN_FACTOR = 0.2
LEG_POS_DEVIATE_MIN_FACTOR_TERRAIN_LEVEL = 2
WHEEL_RATE_INIT_WEIGHT = -0.5
WHEEL_RATE_SCALE_LEVELS = 3
WHEEL_RATE_MIN_FACTOR = 0.2
WHEEL_RATE_MIN_FACTOR_TERRAIN_LEVEL = 3

# Base height scan settings.
BASE_HEIGHT_THRESHOLD = 0.35

# Cripple target.
CRIPPLE_TARGET = [-0.3, 1.4, -2.5, 0.0, 0.3, 1.4, -2.5, 0.0]
